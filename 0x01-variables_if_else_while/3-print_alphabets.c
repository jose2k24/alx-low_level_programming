#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *  * main - main block
 *   * Return: Always 0 (Success)
**/
int main(void)
{
char c;
for (c = 'a'; c <= 'z'; c++)
{
putchar(low);
}
for (c = 'A'; c <= 'Z'; c++)
{
putchar(low);
}
{
putchar('\n');
}
return (0);
}
