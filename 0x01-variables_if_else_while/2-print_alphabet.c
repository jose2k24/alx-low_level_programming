#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *  * main - main block
 *   * Return: Always 0 (Success)
**/
int main(void)
{
	char low;
        for (low = 'a'; low <= 'z'; low++)
	{ putchar(low);
	}
   	   return (0);
}
